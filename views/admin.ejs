<!DOCTYPE html>
<html lang="en" class="bg-gray-900 text-gray-100">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Admin Dashboard | T.Nestia</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen flex flex-col">

  <!-- Header -->
  <header class="flex items-center justify-between bg-gray-800 p-4 shadow-md">
    <h1 class="text-xl font-semibold">🛠️ Admin Dashboard</h1>
    <div>
      <span class="mr-4">Welcome, <%= username %></span>
      <button
        onclick="location.href='http://localhost:5000/'"
        style="
          padding: 6px 10px;
          border: none;
          background: #333;
          color: white;
          border-radius: 5px;
        "
      >
        Logout
      </button>
    </div>
  </header>

  <!-- Main content -->
  <main class="flex-grow p-6 max-w-7xl mx-auto">
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-6 mb-8">

      <div class="bg-gray-700 rounded-lg p-6 shadow-lg text-center">
        <h2 class="text-lg font-semibold mb-2">Total Users</h2>
        <p class="text-3xl font-bold"><%= stats.totalUsers %></p>
      </div>

      <div class="bg-gray-700 rounded-lg p-6 shadow-lg text-center">
        <h2 class="text-lg font-semibold mb-2">Active Students</h2>
        <p class="text-3xl font-bold"><%= stats.activeStudents %></p>
      </div>

      <div class="bg-gray-700 rounded-lg p-6 shadow-lg text-center">
        <h2 class="text-lg font-semibold mb-2">Active Teachers</h2>
        <p class="text-3xl font-bold"><%= stats.activeTeachers %></p>
      </div>

      <div class="bg-gray-700 rounded-lg p-6 shadow-lg text-center">
        <h2 class="text-lg font-semibold mb-2">Pending Approvals</h2>
        <p class="text-3xl font-bold"><%= stats.pendingApprovals %></p>
      </div>

    </div>

    <div class="flex flex-wrap gap-4">
      <button
        class="bg-blue-600 hover:bg-blue-700 text-white px-5 py-3 rounded shadow"
        onclick="alert('👥 Manage Users clicked!')"
      >
        Manage Users
      </button>

      <button
        class="bg-green-600 hover:bg-green-700 text-white px-5 py-3 rounded shadow"
        onclick="alert('📊 View Reports clicked!')"
      >
        View Reports
      </button>

      <button
        class="bg-yellow-600 hover:bg-yellow-700 text-white px-5 py-3 rounded shadow"
        onclick="alert('⚙️ System Settings clicked!')"
      >
        System Settings
      </button>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-800 p-4 text-center text-sm text-gray-400">
    &copy; 2025 T.Nestia. All rights reserved.
  </footer>

</body>
</html>
